const router = require('express').Router();
const controller = require('../controllers');

// router.get('/view-meme', controller.);
// router.post('/add-meme', controller.);

module.exports = router;